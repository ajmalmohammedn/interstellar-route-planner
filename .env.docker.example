# PostgreSQL Configuration
POSTGRES_DB=interstellar
POSTGRES_USER=interstellar_test_user
POSTGRES_PASSWORD=interstellar_test_password

# Django Configuration
DEBUG=True
SECRET_KEY='django-insecure-tw-u8r=$7z7^9!8s2ikt=nxuz&@&hwknjk2&s2)4hl14rg9$1^'
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Connection (for Django)
DB_NAME=interstellar
DB_USER=interstellar_test_user
DB_PASSWORD=interstellar_test_password
DB_HOST=db
DB_PORT=5432

# Instructions:
# 1. Copy this file to .env.docker: cp .env.docker.example .env.docker
# 2. Update SECRET_KEY with a real one generated by: 
#    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# 3. Change passwords to secure values for production use
